#include <iostream>
#include <vector>
int main() {
    std::vector<std::string> k = {"x", "y", "z"};
    for (int i = 0; i < k.size(); ++i) {
        std::cout << k[i];
        if (i < k.size() - 1) std::cout << " ";
    }
    return 0;
}
